{% set logo %}
  {% include "../svg/logo-with-name-no-text.min.svg" %}
{% endset %}

<div class="nav-fixed bg-lightergray">
  {% if full %}
    <nav class="border-b-2 border-lightgray flex flex-col items-center justify-between sm:flex-row m-auto py-4 w-full px-4" id="head-navigation">
  {% elif wide %}
    <nav class="border-b-2 border-lightgray flex flex-col items-center justify-between sm:flex-row m-auto py-4 w-full max-w-screen-md xxl:max-w-screen-lg px-4 md:px-0" id="head-navigation">
  {% else %}
    <nav class="border-b-2 border-lightgray flex flex-col items-center justify-between sm:flex-row m-auto py-4 w-full max-w-screen-md px-4 md:px-0" id="head-navigation">
  {% endif %}
    <div class="mb-3 sm:mb-0">
      <a href="/">
        <span class="block w-24">
          {{ logo | svgo | safe }}
        </span>
      </a>
    </div>
    <ul class="mt-3 ml-0 sm:mt-0 flex list-none space-x-5">
      {% for nav in navigation -%}
        {% if page.url.startsWith(nav.url) %}
          <li class="m-0">
            <a class="active-nav no-underline py-1" href="{{ nav.url }}">{{ nav.title }}</a>
          </li>
        {% else %}
          <li class="m-0">
            <a class="unactive-nav no-underline py-1" href="{{ nav.url }}">{{ nav.title }}</a>
          </li>
        {% endif %}
      {%- endfor %}
    </div>
  </nav>
</div>
